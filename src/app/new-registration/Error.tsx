import React from 'react';
import ErrorFallback from '@/components/common/Error';

type Props = {};

const Error = (props: Props) => {
  return <ErrorFallback className="mt-20" />;
};

export default Error;
