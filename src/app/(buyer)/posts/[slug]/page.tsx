import BoxedContent from '@/components/common/BoxedContent';

export default function Post({ params }: { params: { slug: string } }){

  return (
    <BoxedContent className="gap-x-5 py-24">testing</BoxedContent>

  )
}